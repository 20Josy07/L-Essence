[build]
  command = "npm run build"
  publish = "dist"

# Redirige todas las rutas a index.html para que React Router (SPA) las maneje.
# Sin esto, recargar /admin, /catalogo, etc. da error 404.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
